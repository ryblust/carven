import .values using { base, published, };

export const combined: i32 = base + published;

export fn constants_cross_lambda_without_capture() -> i32 {
    const local: i32 = 1;
    let read = []() => combined + local;
    return read();
}
