import .types using { Model, Choice, Code, Failure, };

export fn inspect(
    model: Model,
    _: Choice,
    _: Code,
    callback: fn(Model) -> Model,
) -> Model throw Failure => callback(model);
