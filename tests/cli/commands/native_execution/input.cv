import "arguments.hpp";

const fn prepare() {
    println("compiled");
    return 1;
}

const prepared = prepare();

const test "static execution" {
    println("static test");
}

const {
    println("constant block");
}

test "unselected test entry" {
    println("unexpected runtime test");
}

struct Failure {}

fn main(args) throw Failure {
    println("running", prepared);
    ::show_args(args);
    throw Failure {};
}
