import std::utf.text using from_utf8;
import std::utf.validation using UTF8Error;
import demo::api using answer;

fn main() throw UTF8Error {
    let bytes: [u8; 2] = [0x6f, 0x6b];
    println(from_utf8(bytes)?);
    println(answer());
}
