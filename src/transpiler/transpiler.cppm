export module zero.transpiler;

import zero.transpiler.token;
import zero.transpiler.lexer;
import zero.transpiler.parser;
import zero.transpiler.codegen;

import std;

export constexpr auto transpile(std::string_view source) noexcept -> std::string {
    return "TODO: Unimplemented function transpile";
}