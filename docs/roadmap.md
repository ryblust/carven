# 特性路线图

已实现的特性见 [grammar.md](grammar.md)。

## 进行中

| 特性 | 状态 |
|------|------|
| Range for 循环 `for i in 1..10` | proposed |
| 文档注释 `//!` / `///` | proposed |
| 内联测试 | proposed |
| 内联 C++ 代码 | proposed |

## 未来方向

- 结构体字面量 `Point { x: 1, y: 2 }`
- 泛型 / 模板
- Trait / 接口
- Lambda 表达式
- 枚举变体携带数据
- 模块级 `const` 和 `static`
- `extern` 函数声明
- 多文件项目支持
- `export` 模块导出
- `as` 类型转换
